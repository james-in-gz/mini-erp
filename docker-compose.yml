services:
  backend:
    build: ./backend
    container_name: crm_backend
    ports:
      - "8080:8080"
    env_file:
      - .env
    restart: always

  frontend:
    build: ./frontend
    container_name: crm_frontend
    ports:
      - "3000:80"
    restart: always